dependencies:
  espressif/led_strip: "^2.4.1"
  espressif/mdns:
    version: "^1.0.3"
    rules:
      - if: "idf_version >=5.0"
  espressif/esp-dl:
    version: ">=1.0.0"  # Use the appropriate version based on the latest available version
